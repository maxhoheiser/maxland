"""
probability calculator
"""
class ProbabilityConstuctor():
    def __init__(self, settings)

        #calculate in numpy array

        #list of dictionaries
        #trial 1:  

        if probability.gambl_left:
            # check for probability of big reard
            if probability.gambl_reward:
                # big rewaerd
            else:
                # no reward
        elif probability.safe_reward:
            # self.small reward
            )
        else:



    gambl_left:
